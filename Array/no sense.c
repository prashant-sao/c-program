#include<stdio.h>
	int i,n;
int unique(int arr){
	for(i=0;i<n;i++){
		for(n=1;n<n-1;n++){
			if(arr[i]==arr[i+n]){
				printf("%d",arr[i]);
			}
			else{
				printf("%d",arr[i]);
			}
		}
		
	}
}

int main(){

	int arr[i];
	for(i=0;i<n;i++){
		printf("Element %d is ",i);
		scanf("%d",&arr[i]);
	}
	int unique(int arr[i]);
}
